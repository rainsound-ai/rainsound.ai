{
  "name": "serverless_functions",
  "version": "0.0.1",
  "scripts": {
    "deploy": "cargo build --release && wrangler deploy",
    "dev": "cargo build --release && wrangler dev"
  },
  "dependencies": {
    "wrangler": "3.8.0"
  }
}
